// players
export const getPlayers = (state) => Object.values(state.players);

// playerTable
export const getPlayerTable = (state) => state.playerTable;

// playerInfoDialog
export const getPlayerInfoDialog = (state) => state.playerInfoDialog;

// playerInfoDialog
export const getDeletePlayerDialog = (state) => state.deletePlayerDialog;
